@model EmpleadosModel;

@{
    ViewData["Title"] = "Borrar Empleado";
}

<form asp-controller="Empleados" asp-action="EmpleadosDelete">

    <div class="card" style="width: 18rem">
        <div class="card-body">
            <h5 class="card-title">Informacion</h5>
            <h6 class="card-subtitle mb-2 text-body-secundary">¿Desea borrar al empleado?</h6>
            <p class="card-text">Nombre: @Model.Nombre</p>
        </div>
    </div>

    <input type="hidden" asp-for="IdEmpleados"/>
    <button type="submit" class="btn btn-danger">Borrar</button>
    <a class="btn btn-secundary" asp-action="EmpleadosList" asp-controller="Empleados">
        Cancelar
    </a>
</form>